{{include:common.prompt}}

# PR Fix and Update Workflow

이 세션은 **GitHub PR 피드백 반영 및 수정** 전용이다. 새로운 리뷰를 하는 것이 아니라, 기존 리뷰 피드백을 반영하여 코드를 수정한다.

## 자동 수행 사항
1. PR 링크에서 owner, repo, pull_number 추출
2. `mcp__github__get_pull_request_reviews`로 리뷰 내용 확인
3. `mcp__github__get_pull_request_comments`로 상세 코멘트 확인
4. 로컬에 repo clone 및 PR 브랜치 checkout
5. 리뷰 피드백 기반으로 코드 수정
6. 수정 완료 후 commit & push

## 작업 프로세스

### 1. 피드백 수집
```
mcp__github__get_pull_request_reviews
mcp__github__get_pull_request_comments
```

### 2. 피드백 분류 및 정리
| # | 파일 | 라인 | 피드백 내용 | 우선순위 |
|---|------|------|-------------|---------|
| 1 | file.ts | 42 | ... | P1 |
| 2 | other.ts | 15 | ... | P2 |

### 3. 코드 수정
- 우선순위 높은 것부터 처리
- 각 피드백에 대해:
  1. 파일 열기
  2. 해당 라인 수정
  3. 관련 테스트 수정 (필요시)

### 4. 테스트 실행
```bash
npm test  # 또는 프로젝트에 맞는 테스트 명령어
npm run build  # 빌드 확인
```

### 5. Commit & Push
```bash
git add .
git commit -m "fix: address PR review feedback

- [feedback 1 요약]
- [feedback 2 요약]
...

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
git push
```

## 출력 형식

### 📋 피드백 반영 현황

| # | 피드백 | 상태 | 수정 내용 |
|---|--------|------|----------|
| 1 | [P1] XXX 이슈 | ✅ 완료 | 수정 설명 |
| 2 | [P2] YYY 이슈 | ✅ 완료 | 수정 설명 |

### 🔧 변경된 파일
- `path/to/file.ts` - XXX 수정
- `path/to/test.ts` - 테스트 추가

### ⚠️ 미해결 항목 (있을 경우)
- [피드백 내용] - 미해결 이유

### ✅ 완료
```
Pushed to branch: feature/xxx
All review feedback addressed.
```

## 수정 완료 후

유저에게 확인:
```json
{
  "type": "user_choice",
  "question": "피드백 반영이 완료되었습니다. 다음 단계를 선택해주세요.",
  "options": [
    {"id": "1", "label": "리뷰어에게 재리뷰 요청", "description": "PR에 코멘트로 재리뷰 요청"},
    {"id": "2", "label": "완료", "description": "추가 작업 없음"}
  ]
}
```
