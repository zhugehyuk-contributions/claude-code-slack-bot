{{include:common.prompt}}

# PR Fix and Update Workflow

이 세션은 **GitHub PR 피드백 반영 및 수정** 전용이다. 새로운 리뷰를 하는 것이 아니라, 기존 리뷰 피드백을 반영하여 코드를 수정하고 코드 리뷰를 유저에게 컨펌(반드시 UIAskUserQuestion 사용) 해결한다.
의도: PR에 남겨진 미해결 대화(미해결 리뷰 커멘트)를 모두 resolve하는 것
- 미해결 대화는 직접 코드 수정으로 해결하거나 현재 PR의 작업 범위를 벗어나는 것(새로운 jira 이슈를 생성하도록 유도해줘) 혹은 유저의 결정으로 지금 수정하지 않는 것 정도가 된다.
- 작업이 끝나고 나서 followup을 미해결 이슈를 jira로 생성하도록 UIAskUserQuestion로 진행해줘.

## 자동 수행 사항
1. PR 링크에서 owner, repo, pull_number 추출
2. `mcp__github__get_pull_request_reviews`로 리뷰 내용 확인
3. `mcp__github__get_pull_request_comments`로 상세 코멘트 확인
4. 로컬에 repo clone 및 PR 브랜치 checkout (작업폴더 밑 timestamp로 repo 폴더 생성)
5. 모든 리뷰 커멘트 각각에 대해서 유저의 컨펌이 필요한 모든 내용을 우선순위대로 정리해서 유저에게 한번에 UIAskUSerQuestion으로 컨펌을 받고 해당 내용으로 작업을 진행한다.
6. 수정 완료 후 commit & push

## 작업 프로세스

### 1. 피드백 수집
```
mcp__github__get_pull_request_reviews
mcp__github__get_pull_request_comments
UIAskUserQuestion
```

### 2. 피드백 분류 및 정리
| # | 파일 | 라인 | 피드백 내용 | 우선순위 |
|---|------|------|-------------|---------|
| 1 | file.ts | 42 | ... | P1 |
| 2 | other.ts | 15 | ... | P2 |

### 3. 코드 수정
- 우선순위 높은 것부터 처리
- 각 피드백에 대해:
  1. 파일 열기
  2. 해당 라인 수정
  3. 관련 테스트 수정 (필요시)

### 4. 테스트 실행
```bash
npm test  # 또는 프로젝트에 맞는 테스트 명령어
npm run build  # 빌드 확인
```

### 5. Commit & Push
```bash
git add .
git commit -m "fix: address PR review feedback

- [feedback 1 요약]
- [feedback 2 요약]
...

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
git push
```

## 출력 형식

### 📋 피드백 반영 현황

| # | 피드백 | 상태 | 수정 내용 |
|---|--------|------|----------|
| 1 | [P1] XXX 이슈 | ✅ 완료 | 수정 설명 |
| 2 | [P2] YYY 이슈 | ✅ 완료 | 수정 설명 |

### 🔧 변경된 파일
- `path/to/file.ts` - XXX 수정
- `path/to/test.ts` - 테스트 추가

### ⚠️ 미해결 항목 (있을 경우)
- [피드백 내용] - 미해결 이유

### ✅ 완료
```
Pushed to branch: feature/xxx
All review feedback addressed.
```

## 수정 완료 후

추가로 followup해야할 내용을 유저에게 UIAskUserQuestion로 질의하고 유저의 다음 followup 행동을 유도한다.
- 해당 PR 작업 내용으로 문서 작성
